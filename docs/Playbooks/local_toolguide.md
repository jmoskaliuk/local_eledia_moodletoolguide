# (verschoben)

Der Playbook-Inhalt ist seit v1.1.40 in
[`../00-master.md`](../00-master.md) §14–§23 konsolidiert.

Der `Playbooks/`-Ordner kann nach erfolgreichem Push entfernt werden —
in der Cowork-Sandbox geht `rm` nicht, deshalb steht hier dieser
Redirect-Stub. Aus dem Host-Terminal:

```bash
rm -rf docs/Playbooks/
```
